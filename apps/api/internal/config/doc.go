// Package config loads and validates process configuration from the environment.
//
// It depends exclusively on the standard library and exposes
// technology-neutral values consumed by the composition root and adapters.
package config
